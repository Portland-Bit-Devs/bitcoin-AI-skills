# bitcoin-cli

Drive a Bitcoin Core node from the command line.

**Status:** stub — structure only, reference material not yet written.

```bash
claude plugin install bitcoin-cli@bitcoin-ai-skills
```

Provides the `bitcoin-cli` skill: `bitcoin-cli` invocation and `bitcoin.conf`, the
JSON-RPC method surface, network selection (mainnet/testnet/signet/regtest), and reading
RPC output correctly.

Licensed GPL-3.0. See the [repository root](../../README.md).
