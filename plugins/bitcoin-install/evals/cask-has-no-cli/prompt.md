I installed Bitcoin Core on my Mac with Homebrew:

```
brew install --cask bitcoin-core
```

The app runs fine and it's syncing. But in my terminal:

```
$ bitcoin-cli getblockcount
zsh: command not found: bitcoin-cli
```

Did the install fail? Should I reinstall it?
